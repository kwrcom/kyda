# PostgreSQL Configuration for MLflow
# Reason: MLflow uses PostgreSQL to store experiment metadata
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=mlflow

# Airflow PostgreSQL Configuration
# Reason: Airflow requires separate database for workflow metadata
AIRFLOW_POSTGRES_USER=airflow
AIRFLOW_POSTGRES_PASSWORD=airflow
AIRFLOW_POSTGRES_DB=airflow

# MinIO Configuration
# Reason: S3-compatible storage for MLflow artifacts (models, plots, etc.)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET=mlflow

# Airflow Configuration
# Reason: CeleryExecutor settings for distributed task execution
AIRFLOW__CORE__EXECUTOR=CeleryExecutor
AIRFLOW__CORE__FERNET_KEY=ZmDfcTF7_60GrrY167zsiPd67pEvs0aGOv2oasOM1Pg=
AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=true
AIRFLOW__CORE__LOAD_EXAMPLES=false

# Airflow Admin User
# Reason: Initial admin credentials for first login
AIRFLOW_ADMIN_USERNAME=admin
AIRFLOW_ADMIN_PASSWORD=admin
AIRFLOW_ADMIN_EMAIL=admin@kyda.tech

# Kafka Configuration
# Reason: Message broker for transaction streaming
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
KAFKA_TOPIC=transactions
TRANSACTIONS_PER_SECOND=10

# Backend API Configuration
# Reason: JWT and database settings for backend service
JWT_ALGORITHM=RS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
